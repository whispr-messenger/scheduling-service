# Monitoring des files

## Endpoint

```
GET /api/v1/monitoring/queues
```

## Métriques par file

```
Queue ──▶ Waiting (en attente)
     ──▶ Active (en cours)
     ──▶ Completed (terminés)
     ──▶ Failed (échoués)
```
