apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: whispr-dev

resources:
  - ../../base

commonLabels:
  environment: dev

patches:
  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: scheduling-service
      spec:
        replicas: 1
        template:
          spec:
            containers:
              - name: scheduling-service
                resources:
                  requests:
                    cpu: "100m"
                    memory: "256Mi"
                  limits:
                    cpu: "500m"
                    memory: "512Mi"
  - patch: |-
      apiVersion: autoscaling/v2
      kind: HorizontalPodAutoscaler
      metadata:
        name: scheduling-service
      spec:
        minReplicas: 1
        maxReplicas: 3

configMapGenerator:
  - name: scheduling-service-config
    behavior: merge
    literals:
      - REDIS_HOST=scheduling-redis-dev
      - BULL_BOARD_ENABLED=true
