apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: whispr-staging

resources:
  - ../../base

commonLabels:
  environment: staging

patches:
  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: scheduling-service
      spec:
        replicas: 2
        template:
          spec:
            containers:
              - name: scheduling-service
                resources:
                  requests:
                    cpu: "200m"
                    memory: "384Mi"
                  limits:
                    cpu: "750m"
                    memory: "768Mi"
  - patch: |-
      apiVersion: autoscaling/v2
      kind: HorizontalPodAutoscaler
      metadata:
        name: scheduling-service
      spec:
        minReplicas: 2
        maxReplicas: 5

configMapGenerator:
  - name: scheduling-service-config
    behavior: merge
    literals:
      - REDIS_HOST=scheduling-redis-staging
      - BULL_BOARD_ENABLED=false
